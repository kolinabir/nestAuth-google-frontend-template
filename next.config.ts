import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["lh3.googleusercontent.com", "www.google.com", "i.postimg.cc"],
  },
  /* other config options here */
};

export default nextConfig;
